version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
        # - npm install -g yarn
        # - pnpm i
        - npm i
    build:
      commands:
        # - pnpm run build
        - npm run build
  artifacts:
    # baseDirectory: .next
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

# commands:
# aws amplify update-app --app-id dnah9h2g1p49g --platform WEB_DYNAMIC --region ap-southeast-2 
# aws amplify update-branch --app-id dnah9h2g1p49g --branch-name main --framework 'Next.js - SSR' --region ap-southeast-2